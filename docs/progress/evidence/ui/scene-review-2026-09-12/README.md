# 通常入口からのシーン確認

e181dfbの配信buildでscene-journey.spec.tsを実行。
Chromium/Firefox/WebKit × Desktop1440×900・タッチ667×375の6件成功。

タイトル演出終了→ロビーで名前入力→設定でmute→倍率切替→75%で練習開始→
開幕終了→降参→勝敗と表情→ロビー帰還→名前とmute保持を確認した。
ゲーム画面の100分割目盛り、JS例外なしも確認。
小画面設定ではパネル内をスクロールして倍率selectまで操作できた。

all-scenes.jpgはWebKit実行で保存した5シーン×2画面サイズを縮小して並べたもの。
初回取得ではタイトルのイントロ途中が写っていたため、演出完了を待つ条件を追加し
再実行した。ロゴ自体の透明度を変更したわけではない。

この一覧は配色・レイアウトを比較する資料であり、縮小後の文字サイズや細部を
実機の視認性の証明には使わない。最終UIの承認・実機性能・公開前判断は未完。
本番公開・main統合は未実施。
