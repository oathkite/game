# 画像地形導入後の統合検証

全体テストを再実行して、個別検証では検出できていなかった2件を修正した。

## 素材の出自と圧縮

岩肌を更新した601bc3fで、runtime画像だけが変更され、manifestとencoderが
旧terrain-cliff-v4を参照したままだった。全体検証はStale runtime artworkで停止した。
現行元画像ui/terrain-2026-09-12/rock.pngを登録し、既存のwebp-q94規則へ統一。
RGBのRMSは2.264/1.486/3.051、alpha差0、寸法一致。13素材の再生成・check成功。
他12点のruntime画像は変化なし。岩肌は199,594→336,794Bとなった。
画像地形の3マップはこのタイル素材とは別で、各map画像とmaskを維持する。

## 葦の丘の相互射程

風+10、2人対戦で右側x345から左側x55への標準砲が届かず、既存の
spawnReachability試験が失敗した。追加調査でも照準候補を細かくして届かなかった。
開始位置を75〜325へ移動し、全3マップ・2〜8人・風−10/0/+10の
全席間到達試験9件が成功。移動・空洞着地などmaps95件も成功。

配布済みデータを上書きしないため、配置変更をreed-hills v4として登録。
地形画像はv3と同一。v3保存試合は55/345の旧配置・旧versionで復元することを
テストで確認した。v2保存地形の互換性も維持する。

## 検証結果と範囲

- pnpm test成功：protocol27、sim110、maps95、engine156、client242、server79。
  edge専用4件はskip。素材の自動検証も通過。
- 全対象pnpm typecheck成功。
- 全体試験の収集後に追加したv3配置復元ケースを含むbattleSnapshot10件も別実行で成功。
- 葦の丘v4の配信build＋local SQLite DOで、射撃・破壊地形共有・再接続・カメラ初期座標を2contextで検証。Chromium14.8秒、Firefox15.7秒、WebKit14.0秒で全3件成功。
- 本検証は実機・実地域・本番DO容量の証明ではない。
- 全体のUI原案一致、最終art承認、実機/公開前判断は継続。本番公開・main統合は未実施。
