# UIの枠線

- 見出しや内容ブロックを囲う装飾用の枠線は使わない。
- ボタン・入力欄・選択状態を示す枠線は2pxで統一する。
- 車体色・砲塔色のスウォッチは常に2pxの枠領域を確保し、未選択は透明、選択中はテーマの緑（#33ff66）にする。選択によってサイズを変えない。
- キーボードのフォーカス表示は、選択状態とは別に維持する。
- 三角形やドット絵を描くためのCSS borderは、UI枠線の対象外。
