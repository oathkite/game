# 18. プロフィール・リザルト用キャラクター

2026-09-09。作業ブランチ：codex/2d-avatar-portraits。総合ブランチ：codex/2d-update。
本章は立ち絵の制作規格。搭乗用192×160のatlasとは別のportrait形式を定義する。
所有区分・カスタマイズの正本は[17章](17-avatar-customization.md)。今回は本人専用KITAの制作プレビューであり、一般用アバターではない。

## 18.1 画面ごとの役割

|画面|必要素材|体験|
|---|---|---|
|プロフィール|基本立ち絵、待機loop、顔アイコン|自分の外見と装備を落ち着いて見せる|
|勝利リザルト|喜ぶ導入、勝利後の待機|決着の喜びから落ち着いた表示へ移る|
|敗北リザルト|悲しむ導入、敗北後の待機|悔しさ・落胆を短く表現し、次の試合へ向かう|
|入室・挨拶|手を振る短い動き|プレイヤーの存在を伝える|
|動きを抑える表示|各状態の代表静止画|アニメーションなしでも情報と表情が伝わる|

リザルトは「導入のone-shot → 状態別idle loop」。勝利のジャンプや敗北への落ち込みを無限に繰り返さない。
プロフィールとリザルトのUIは確認用モックアップで、実ゲームの画面・戦績・アカウント連携は未実装。

## 18.2 今回の制作範囲

|ID|今回の成果物|次の段階|
|---|---|---|
|neutral|基本立ち絵1枚|呼吸・まばたき|
|happy|両手を上げた喜び1枚|喜びへの導入と勝利後の待機|
|sad|肩を落とした悲しみ1枚|悲しみへの導入と敗北後の待機|
|face-icon|未制作|小さい表示専用の顔構図|
|wave|未制作|短い挨拶|

3ポーズ×素体・メガネ・スカーフの9レイヤーフレームを作成。装備を重ねた3枚だけを納品する形式にはしない。
今回の静止画を「アニメーション完成」と記録しない。中間コマは体形・表情の承認後に追加する。

## 18.3 portrait形式 v1

- manifest：`avatar.json`、`avatarPortraitVersion: 1`。機体用`pack.json`と別形式。
- frameSize：192×224 artpx、足元基準foot=[96,208]。立ち絵は両目の見える、少し右を向いた前方3/4 view。搭乗用の真横 viewとは用途を分ける。
- 各レイヤーatlas：576×224（横3コマ）、neutral=0、happy=1、sad=2。
- 画像：32色indexed PNG、index0だけ透明、それ以外は不透明。編集用ORAを併納。
- ドット表示：nearest、devicePixelRatioを考慮した整数倍率。立ち絵は承認された元デザイン案に近い密度で制作する。搭乗用の低解像度へ固定しない。体形・配色を共通にし、用途別に解像度を持つ。
- bodyの原画縮尺は全ポーズ0.30。装備は前方3/4 viewの原画を使い、glasses=0.30、scarf=0.28のレイヤー別縮尺で出力する。真横向きの搭乗用パーツをそのまま流用しない。装備単位では同率拡縮を許可し、ポーズごとに大きさを変えない。
- 生成画像は実際の不透明範囲を測定して切り出す。3等分の機械的cropでは、喜ぶポーズの手を切り落とすため、`registration.json`の明示領域を使用する。
- bodyは原画の足元とfootを対応付ける。sadの低い頭を、拡大してneutralと同じ高さへ合わせない。
- 描画順：body → scarf → glasses。影・文字・勝利の紙吹雪は原画に焼き込まない。
- 本人用paletteは固定。立ち絵を色替えして一般用として公開しない。

manifestにはid、status、availability、model、frameSize、foot、scale、poses、layers、metrics、view、animationStatus、humanReviewを記録する。
posesはid、label、frame、eyeAreas、faceArea、headAnchor、neckAnchorを持つ。eyeAreasは左右の自然の目を別々の矩形で記録する。
faceAreaは顔周辺の参考領域であり、目の遮蔽判定はeyeAreasを使う。白い喉・腹を目の保護領域として扱わない。
各レイヤーはfileとsource（ORA）を持つ。metricsはcrop領域・不透明bounds・出力サイズ・出力位置・縮尺・原画SHA256を持つ。
availabilityはowner-only、grantKey=kita-personal。これは素材の利用区分であり、制作ツールに認証を実装したという意味ではない。

## 18.4 装備接続と改善記録

最新の方向性（密度の追加フィードバックを反映）：立ち絵は元デザイン案に近い描き込み密度を許可する。現行の素体は約111〜124×158〜167ドット。64×64版の拡大画像ではなく、生成原画から再出力する。

添付のNEW DIRECTION 01にあるカエルの丸い体形を基準に、立ち絵・喜び・悲しみは若干斜めの前方3/4 viewへ変更。全ポーズで両目が見える同じカメラを保つ。ドット密度をそろえることと、搭乗用と同じ真横アングルにすることは別の要件。表情の読みやすさと短い手足を優先する。


経緯：初案は脚が長かったため体形を修正。その後、幅25〜35・高さ40〜46ドットの低解像度を試したが、最新フィードバックで元デザイン案に近い密度へ変更した。短い脚と丸い胴体は維持する。sadはうつむくため高さを縮め、別倍率で引き伸ばさない。旧原画はposes-tall.png、真横の試作はposes-side.pngとして制作履歴に残す。


初回生成のメガネは自然の目に重なった。上へ移動するだけでは頭から浮き、下へ移すと喉へ装着したように見えた。
対策としてメガネを全ポーズ共通の縮尺で小さくし、頭の上に接する位置へ登録した。素体の目と表情は表示を保つ。
スカーフは首元を覆い、輪郭と結び目を持つ別レイヤーにした。外した状態に装備の線を残さない。

今回の装備は立ち絵の角度に合わせた原画を使用し、出力位置の正本はregistrationとmetrics。headAnchor/neckAnchorは目視ガイド。
次のパーツを追加するときは、attachとposeごとの接続点を明示する。既存ガイドの位置へ新規パーツを置くだけで互換としない。

## 18.5 確認ツール

`http://127.0.0.1:4178/avatar.html`（アバター工房）。既存の機体確認ツールからリンクする。

- 立ち絵・喜ぶ・悲しむを即時切り替え。
- メガネ・スカーフを独立にON/OFF。
- ネイビー／クリームの背景で輪郭を比較。
- 目の保護領域・接続点・足元をガイド表示。
- プロフィール・勝利・敗北のモックアップを同じ素材で描画。
- 全3ポーズ×装備4通りを描画し、検査reportを保存。

無意味な上下揺れで未制作アニメーションを完成に見せない。今回は静止原画の確認に限定し、再生ボタンは設けない。

## 18.6 検証と納品

|検査|方法|
|---|---|
|形式・出典|indexed PNG、atlas寸法、ORA存在、原画SHA256、GPT Image 2.5のmodel ID|
|共通縮尺・接地|全bodyの同率縮小、足元が基準から2 artpx以内|
|切れ|各レイヤーの不透明画素がフレーム端に触れない|
|装備接触|メガネ・スカーフと素体の不透明画素が交差|
|目の保護|左右のeyeAreasに装備の不透明画素が重ならない|
|組み合わせ|3ポーズ×装備4通りで12種類の描画結果|
|UI|ポーズ・装備切替、report保存、モバイル幅、DPR対応|
|目視|自然な体形・表情・装備の装着・用途別の読みやすさ|

画素の接触だけでは自然な装着感を証明できない。顔領域は人が登録した矩形であり、人体構造を自動認識する検査ではない。
現段階はdraft / humanReview=pending。本人専用アバターとしての最終承認と、一般向けの権限管理は別工程。

コマンド：

```sh
pnpm assets:export:avatar
pnpm assets:validate:avatar
pnpm assets:capture:avatar
pnpm assets:test
pnpm assets:test:browser
```

exportはsharpを必要とし、既存の制作環境ではASSET_SHARP_PATHでインストール済みのsharpを指定する。validatorとUnit testsはNode標準機能で動く。

## 18.7 次の制作

静止3ポーズの方向性確認 → 顔アイコン → 待機・勝敗後idle → 喜び・悲しみの導入 → 手を振る、の順に進める。
アニメーション追加時は、開始／終了ポーズ、足元、装備接続、loop境界、静止表示fallbackをmanifestとvalidatorに追加する。
一般用アバターや別種族は本人用素材とは別のasset IDで制作し、共通のportrait形式を使用する。
