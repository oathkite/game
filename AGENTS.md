# 2Dアップデートの作業ブランチ

2Dアップデートの総合ブランチは `codex/2d-update`。
今後の2Dアップデートに関する実装・画像アセット・検証ツール・仕様変更は、最新の総合ブランチから `codex/2d-<作業名>` を切って進める。
作業PRのbaseは `codex/2d-update` とする。`main`から作業を始めたり、作業PRを直接`main`へ向けたりしない。
総合ブランチへの統合後も、本番リリースは別の明示的な指示で行う。`main`へのpushは本番デプロイを起動する。

開始前に現在のブランチ・未コミット変更・remoteの状態を確認し、別作業の変更を消さない。
APIキーを含む `.env.imagegen.local` はローカル専用で、Gitへ追加しない。

詳細は [2Dアップデートのブランチ運用](docs/2d-update-workflow.md) と [設計ドキュメント](docs/design/README.md) を参照。
